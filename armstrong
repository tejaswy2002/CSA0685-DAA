#include<stdio.h>
int main()
{
	int n,rem,sum=0,temp;
	printf("enter the value:");
	scanf("%d",&n);
	temp=n;
	while(temp>0)
{
	rem=temp%10;
	sum+=rem*rem;
	temp=temp/10;
}
 if(sum==n)
 printf("armstrong number");
 else
 printf("not armstrong");
}
